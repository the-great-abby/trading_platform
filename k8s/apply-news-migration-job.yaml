apiVersion: batch/v1
kind: Job
metadata:
  name: apply-news-migration-job
  namespace: trading-system
  labels:
    app: news-migration-apply
    job-type: migration
spec:
  template:
    metadata:
      labels:
        app: news-migration-apply
    spec:
      containers:
      - name: news-migration-apply
        image: trading-bot:latest
        imagePullPolicy: Never
        command: ["python"]
        args: ["-m", "alembic", "upgrade", "20250706015740"]
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: database-url
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        volumeMounts:
        - name: shared-data
          mountPath: /app/data
      volumes:
      - name: shared-data
        persistentVolumeClaim:
          claimName: backtest-data-pvc
      restartPolicy: Never
  backoffLimit: 3 