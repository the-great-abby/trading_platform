apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service-simple
  namespace: trading-system
  labels:
    app: order-service-simple
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service-simple
  template:
    metadata:
      labels:
        app: order-service-simple
    spec:
      containers:
      - name: order-service-simple
        image: localhost:32000/order-service:v3
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              key: database_url
              name: trading-secrets
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              key: rabbitmq_url
              name: trading-secrets
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 100m
            memory: 128Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: order-service-simple
  namespace: trading-system
spec:
  selector:
    app: order-service-simple
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
