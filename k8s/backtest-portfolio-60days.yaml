apiVersion: batch/v1
kind: Job
metadata:
  name: backtest-portfolio-60days
  namespace: trading-system
spec:
  template:
    spec:
      containers:
      - name: backtest-portfolio-60days
        image: localhost:5000/trading-system:latest
        command: ["python", "run_portfolio_performance_60days.py"]
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: database-url
        - name: POLYGON_API_KEY
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: polygon-api-key
        - name: OLLAMA_URL
          value: "http://ollama:11434"
        - name: OLLAMA_MODEL
          value: "granite3.3:2b"
        - name: OLLAMA_TIMEOUT
          value: "60"
        - name: OLLAMA_RETRIES
          value: "3"
        - name: OLLAMA_BACKOFF_BASE
          value: "2"
        - name: OLLAMA_BACKOFF_MAX
          value: "30"
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
      restartPolicy: Never
  backoffLimit: 1 