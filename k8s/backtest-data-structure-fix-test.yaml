apiVersion: batch/v1
kind: Job
metadata:
  name: backtest-data-structure-fix-test
  namespace: trading-system
spec:
  template:
    spec:
      serviceAccountName: trading-service-account
      containers:
      - name: backtest
        image: localhost:5000/backtest:latest
        command: ["python", "run_test_data_structure_fix.py"]
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: database-url
        - name: POLYGON_API_KEY
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: polygon-api-key
        - name: USE_LLM_EVALUATION
          value: "false"
        - name: DATABASE_ONLY_MODE
          value: "true"
        resources:
          requests:
            memory: "2Gi"
            cpu: "1000m"
          limits:
            memory: "4Gi"
            cpu: "2000m"
      restartPolicy: Never
  backoffLimit: 2 