apiVersion: batch/v1
kind: Job
metadata:
  name: news-migration-job
  namespace: trading-system
  labels:
    app: news-migration
    job-type: migration
spec:
  template:
    metadata:
      labels:
        app: news-migration
    spec:
      containers:
      - name: news-migration
        image: trading-bot:latest
        imagePullPolicy: Never
        command: ["python"]
        args: ["-m", "alembic", "upgrade", "head"]
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: database-url
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        volumeMounts:
        - name: shared-data
          mountPath: /app/data
      volumes:
      - name: shared-data
        persistentVolumeClaim:
          claimName: backtest-data-pvc
      restartPolicy: Never
  backoffLimit: 3 