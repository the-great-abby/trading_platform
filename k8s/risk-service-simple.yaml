apiVersion: apps/v1
kind: Deployment
metadata:
  name: risk-service-simple
  namespace: trading-system
  labels:
    app: risk-service-simple
spec:
  replicas: 1
  selector:
    matchLabels:
      app: risk-service-simple
  template:
    metadata:
      labels:
        app: risk-service-simple
    spec:
      containers:
      - name: risk-service-simple
        image: localhost:32000/risk-service:v2
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              key: database_url
              name: trading-secrets
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              key: rabbitmq_url
              name: trading-secrets
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 100m
            memory: 128Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 15
          timeoutSeconds: 10
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: risk-service-simple
  namespace: trading-system
spec:
  selector:
    app: risk-service-simple
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
